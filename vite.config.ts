import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    // This bypasses TypeScript strict checking errors during production builds
    sourcemap: false,
  },
});